# Build Tasks: Global UI Refresh

Generated from: `.design/global-ui-refresh/DESIGN_BRIEF.md`  
Date: 2026-06-04

## Foundation

- [x] **Reset the live UI token system to neutral/slate**: Retune the live app color tokens in `src/index.css` and `tailwind.config.ts` so `brand`, `neutral`, `surface`, `canvas`, sidebar, hover, and focus states establish a clear white-on-light-gray hierarchy with slate accents and no warm tan/parchment bias. Done looks like the overall shell immediately reading cooler, quieter, and higher-contrast across existing pages without touching export preview styles. _Modifies: `src/index.css`, `tailwind.config.ts`. Establishes the visual direction from the brief._
- [x] **Collapse live app typography to one sans system plus mono data accents**: Standardize the live UI on `Manrope` for headings, body, labels, and controls, keep `JetBrains Mono` only for IDs/quantities/tabular values, and remove live-app use of `Fraunces`, `Montserrat`, and other decorative heading treatments. Done looks like repeated text roles sharing the same size/weight rules across the shell and major pages. _Modifies: `src/index.css`, `tailwind.config.ts`, any live UI one-off font declarations. Reuses: existing `Manrope` and `JetBrains Mono` imports._
- [x] **Define the calmer shared UI vocabulary**: Refresh `Button`, `Badge`, `SegmentedControl`, and core utility classes like `.card`, `.surface-paper`, `.surface-flat`, `.eyebrow`, and `.table-head-cell` so actions, chips, headings, and containers all share the same quieter professional language. Done looks like the app having one obvious action hierarchy and one consistent surface/treatment vocabulary instead of per-feature variations. _Modifies: `src/components/primitives/Button.tsx`, `Badge.tsx`, `SegmentedControl.tsx`, `src/index.css`. Reuses: existing primitives._

## Core UI

- [x] **Quiet the project shell and navigation chrome**: Apply the new tokens and shared vocabulary to `ProjectHeader`, `ProjectToolSidebar`, `ProjectToolNav`, and related shell/sidebar utilities so the app frame feels consistent before feature pages are restyled. Done looks like Dashboard-to-project transitions feeling like one product, with calmer navigation, lighter surfaces, and cleaner selected/hover states. _Modifies: `src/components/project/ProjectHeader.tsx`, `ProjectToolSidebar.tsx`, `ProjectToolNav.tsx`, relevant sidebar CSS in `src/index.css`. Depends on: Reset the live UI token system to neutral/slate._
- [ ] **Normalize global empty, loading, and modal states**: Update common empty/loading patterns and shell-level modal containers so dashed placeholders, pulsing blocks, and light panel fills stop reintroducing noise or heavy tinting. Done looks like empty/loading states across auth, plans, imports, materials, and shared modals feeling calm and coherent instead of custom per surface. _Modifies: shared empty/loading/modal wrappers in `src/components/shared`, `src/components/primitives`, plus supporting CSS utilities. Reuses: existing modal and surface patterns._
- [ ] **Unify the status language across the live app**: Keep semantic `success`, `warning`, and `danger` colors, but standardize the shape, density, label treatment, and neutral default state for `ItemStatusChip`, `StatusBadge`, and proposal status UI. Done looks like every status reading as the same system no matter which tab or surface it appears in. _Modifies: `src/components/shared/table/ItemStatusChip.tsx`, `src/components/primitives/StatusBadge.tsx`, `src/components/shared/ProposalStatusSelect/*`, related status CSS utilities. Reuses: existing semantic tokens._

## Core Data Views

- [ ] **Retune shared table wrappers and dense row patterns**: Apply the new surface, typography, and spacing rules to shared table shells, sticky headers, bulk bars, and row hover states so dense data views gain stronger contrast and less visual clutter. Done looks like FF&E, Item Library, Budget, and Materials tables sharing one high-clarity scanning model. _Modifies: `src/components/shared/table/TableViewWrappers.tsx`, `TotalsBar.tsx`, `BulkActionBar.tsx`, `ColumnsPanel.tsx`, `generatedItemStickyStyles.ts`, plus related table utility classes. Depends on: Define the calmer shared UI vocabulary._
- [ ] **Refresh the dashboard and project snapshot surfaces first**: Use the lighter hierarchy and new typography on `DashboardPage`, `ProjectSnapshotPage`, and snapshot cards so the top-level product experience validates the direction before deeper feature rollout. Done looks like the first screens users hit already feeling quieter, more spacious, and more professional. _Modifies: `src/pages/DashboardPage.tsx`, `src/pages/ProjectSnapshotPage.tsx`, snapshot card components under `src/components/project/snapshot`. Reuses: refreshed shell, buttons, badges, and surface utilities._
- [ ] **Bring Budget and summary views onto the new system**: Update `BudgetView`, `FfeSummaryView`, and `ProposalSummaryView` so totals, progress bars, tables, and section headers all use the same calm density model instead of mixed feature-specific treatments. Done looks like reporting pages reading cleaner and more authoritative with stronger figure-to-background contrast. _Modifies: `src/components/project/BudgetView.tsx`, `src/components/ffe/summary/SummaryView.tsx`, `src/components/proposal/summary/ProposalSummaryView.tsx`. Depends on: Retune shared table wrappers and dense row patterns._

## Feature Rollout

- [ ] **Restyle Item Library and its focused detail surfaces**: Apply the new hierarchy to the proposal workspace, including grouped rows, spreadsheet view, detail panel, and revision affordances, so the most complex data surface proves the system works under density. Done looks like Item Library feeling calmer, more spacious, and more unified without losing scan speed. _Modifies: `src/components/proposal/table/**/*`, `src/components/proposal/revision/**/*`. Reuses: refreshed tables, status language, buttons, badges, surfaces. Depends on: Retune shared table wrappers and dense row patterns._
- [ ] **Restyle Materials and shared image-heavy panels**: Refresh `MaterialsView`, `MaterialLibraryModal`, `FinishForm`, and shared image/paste surfaces so media-heavy workflows still feel part of the same quiet enterprise system. Done looks like cards, rows, drawers, upload states, and assignment chips all matching the neutral/slate shell without beige fills or noisy accents. _Modifies: `src/components/materials/**/*`, `src/components/shared/image/*`. Reuses: refreshed surfaces, buttons, status language._
- [ ] **Restyle Plans for the desktop/tablet-first workbench**: Apply the updated tokens and typography to `PlansPage`, measured-plan cards, `PlanInspector`, `PlanToolRail`, and `PlanCreateItemPanel` so the plan workspace feels cleaner and less ornamented while staying readable on tablet and desktop. Done looks like the canvas-adjacent UI feeling quieter, lighter, and more operationally precise. _Modifies: `src/pages/PlansPage.tsx`, `src/components/plans/list/**/*`, `src/components/plans/canvas/**/*`. Reuses: refreshed shell, statuses, surfaces, typography._
- [ ] **Align FF&E supporting list and navigator-adjacent UI to the new system**: Refresh the live app FF&E list, navigator overlay rows, supporting dialogs, and list-based maintenance surfaces so they match the global system even though the catalog document styling itself is out of scope. Done looks like FF&E support UI no longer visually diverging from the rest of the app shell. _Modifies: `src/components/ffe/list/FfeItemList.tsx`, supporting overlay/list sections in `src/components/ffe/catalog/CatalogView.tsx`, related shared rows and dialogs. Does not include catalog document page/export preview restyling. Depends on: Retune shared table wrappers and dense row patterns._

## Interactions & States

- [ ] **Run a live-app interaction cleanup pass**: Normalize hover, active, selected, pressed, and focus-visible behavior across shared controls and repeated surfaces so the app feels less noisy and more predictable. Covers: buttons, segmented controls, links, row hovers, cards, tabs, popovers, dropdown items, and empty-state affordances. _Modifies: shared primitives and utility classes only; avoids feature logic changes._
- [ ] **Audit micro-typography and label density across pages**: Remove stray `text-[10px]` / `text-[11px]` role drift where the same conceptual label is styled differently between tabs, and collapse repeated label roles into one system. Covers: eyebrow/meta labels, section labels, table heads, numeric callouts, and muted supporting text. _Modifies: page/component typography call sites across `src/pages` and `src/components`. Depends on: Collapse live app typography to one sans system plus mono data accents._

## Responsive & Polish

- [ ] **Tune the refreshed system for desktop and tablet layouts**: Verify that the new type scale, spacing rhythm, panel paddings, and table/header surfaces feel deliberate on desktop and tablet without becoming cramped or oversized. Breakpoints: `md`, `lg`, `xl`, including sidebars, sticky headers, detail panels, and workspace shells. _Reuses: all refreshed tokens and shared components._
- [ ] **Accessibility pass for the live UI refresh**: Validate contrast, focus visibility, semantic status clarity, and readability of small labels against the lighter neutral shell. Specific checks: high-contrast small text, non-color-only status cues, visible keyboard focus, and no pale gray-on-gray regressions in dense tables and panels. _Covers the full live app UI refresh._

## Review

- [ ] **Design review**: Run a review against `.design/global-ui-refresh/DESIGN_BRIEF.md` after the rollout, with emphasis on consistency across tabs, calmer shell behavior, typography discipline, and reduced background/tint noise.
