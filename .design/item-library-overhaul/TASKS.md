# Build Tasks: Item Library Overhaul

Generated from: `.design/item-library-overhaul/DESIGN_BRIEF.md`
Date: 2026-06-03

## Foundation

- [x] **Reframe the proposal shell as Item Library**: Update the project-tool label, page copy, and proposal sidebar structure so the route still uses `/proposal/table` internally but the user experience reads as Item Library with `View`, `Actions`, `Display`, and `Status` sections. Done looks like a coherent shell with the editorial spec-workspace direction established before deeper row work. _Modifies: `ProjectToolNav`, `ProjectToolSidebar`, `AppBarActions`, proposal route composition in `App.tsx`. Reuses: existing project shell, sidebar primitives, `ProposalStatusSelect`._
- [x] **Add schedule context and selection model**: Introduce a single active-schedule state, main-content header summary, and schedule dropdown with item count and subtotal metadata so the page shows one schedule at a time instead of a continuous multi-category scroll. Done looks like the user can switch schedules without leaving the route and always see the current context clearly. _Modifies: `ProposalTable`, `ProposalCategorySection`, proposal data orchestration. Reuses: existing proposal hooks/data, shared menu/select patterns. Depends on: Reframe the proposal shell as Item Library._

## Core UI

- [x] **Build the default Item Library record layout**: Replace the spreadsheet-style default proposal presentation with a flat FF&E-style strip row that shows item ID/options, rendering, specs, plan, materials, and pricing in one continuous surface with thin dividers and normal page scroll. Done looks like a scan-first list with no primary horizontal-scroll dependency on desktop and no boxed section chrome. _Modifies: `ProposalCategorySection`, `ProposalRow`, shared generated-item cells. Reuses: existing rendering/plan/materials cells, money formatting, row/list surface styles. Depends on: Add schedule context and selection model._
- [x] **Simplify schedule header chrome**: Strip the current per-category header down to schedule identity, count, subtotal, and only the minimum schedule-local controls needed in the default mode. Done looks like actions and column configuration are no longer competing with schedule identity in the content header. _Modifies: `ProposalCategoryHeader`, `ProposalCategorySection`. Reuses: existing badges, inline text edit, money display. Depends on: Build the default Item Library record layout._
- [x] **Move advanced configuration into Spreadsheet View**: Preserve and rename the current expanded/full-screen table as `Spreadsheet View`, keeping all fields editable inline and making it the power-user editing mode rather than the default reading mode. Done looks like dense editing is still available, but clearly secondary to the default Item Library list. _Modifies: `ProposalCategoryExpandedTable`, `ProposalCategoryHeader`, `ProposalTable`. Reuses: existing expanded table, inline cell editors, drag/reorder behavior, column model. Depends on: Add schedule context and selection model._
- [x] **Extend the item detail panel for focused editing**: Keep row click opening the existing detail panel, add custom data there, and make sure full description remains available outside the condensed default list. Done looks like the detail panel is the primary focused editor when the user is not in Spreadsheet View. _Modifies: `ProposalItemDetailPanel`, `ProposalItemDetailForm`, related detail modules. Reuses: existing detail-panel shell and item navigation. Depends on: Build the default Item Library record layout._

## Interactions & States

- [ ] **Implement image-first hover and paste updates**: Add visible hover affordances plus `Ctrl+V` support for rendering, plan image, and swatches in the default list, Spreadsheet View, and detail panel. Done looks like image replacement is explicit, keyboard accessible, and fast without forcing a modal-heavy flow. Covers: hover, keyboard-triggered alternatives, successful paste, invalid clipboard content, upload fallback. _Modifies: shared image/material cells, proposal row surfaces, detail panel media areas, existing paste/upload hooks. Reuses: `MaterialLibraryModal`, image upload flows, existing swatch-paste infrastructure. Depends on: Build the default Item Library record layout._
- [ ] **Add revision mode without polluting default reading**: Move revision comparison behind a dedicated toggle in the sidebar `View` section so before/after pricing appears only when requested. Done looks like open revisions remain actionable, but the default list stays calm and compact. Covers: revision-off default, revision-on comparison state, flagged/resolved summary context. _Modifies: `ProposalTable`, `ProposalCategorySection`, sidebar context in `AppBarActions`, revision components. Reuses: existing revision hooks, summary chip, revision cost/qty/total cells. Depends on: Reframe the proposal shell as Item Library._
- [ ] **Refresh empty, loading, and schedule-edge states**: Redesign empty schedules, empty libraries, schedule switching feedback, and loading/skeleton behavior to match the new single-schedule model. Done looks like the page handles sparse/demo data and real projects gracefully without reverting to spreadsheet assumptions. Covers: no schedules, empty selected schedule, loading selected schedule, import/add entry points. _Modifies: `ProposalEmptyState`, `ProposalTable`, schedule selector/header states. Reuses: existing modals and loading conventions. Depends on: Add schedule context and selection model._

## Responsive & Polish

- [ ] **Adapt Item Library across breakpoints**: Make the grouped record layout collapse cleanly on tablet/mobile, keep the schedule selector usable in the content header, and ensure tool-panel controls remain reachable through the existing mobile project header pattern. Done looks like the default list stacks intelligently instead of becoming a cramped pseudo-table. Breakpoints: desktop, tablet, mobile. _Modifies: proposal list layout, project header/tool-panel interactions as needed. Reuses: current mobile tool-panel pattern, `ProposalCategoryMobileCards` where useful. Depends on: Build the default Item Library record layout._
- [ ] **Accessibility and interaction QA pass**: Audit focus order, keyboard access, contrast, semantic grouping, and non-hover alternatives across the schedule dropdown, default item records, image affordances, detail panel, and Spreadsheet View. Done looks like the redesign preserves the brief’s accessibility requirements rather than adding visual polish only. _Modifies: whichever Item Library components need final fixes. Reuses: existing focus styles and shared primitives. Depends on: all prior Item Library tasks._

## Review

- [ ] **Design review**: Run `/design-review` against the brief after the build lands and capture any must-fix gaps between the implemented Item Library and the editorial spec-workspace direction.
